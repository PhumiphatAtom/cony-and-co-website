import type { Config } from 'tailwindcss';

// Design tokens will be added in Prompt 2.
const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
